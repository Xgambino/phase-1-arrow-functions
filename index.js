// helpers.js

// Function expression called divide
function divide(a, b) {
   
    return a / b;
  }
  
  // Arrow function called square

  const square = x => x * x;
  
  // Arrow function called add
  
  const add = (a, b) => a + b;